<section data-ng-controller="[model:name:plural]CreateController as ccCtrl">

    <div class="modal-header">
        <h3 class="modal-title">New [model:name]</h3>
    </div>
    <div class="modal-body">
        <form name="create[model:name]Form">
            <fieldset>
                <div class="row">
                    <div class="form-group">
                        <div data-ng-show="error" class="text-center text-danger">
                            <div class="alert alert-danger" role="alert">
                                <strong data-ng-bind="error"></strong>
                            </div>
                        </div>
                    </div>
					<div class="col-sm-6 text-center">
                        <i class="glyphicon glyphicon-user [model:name:lower]-profile"
                           ng-class="{'text-primary': ccCtrl.referred, 'text-pink': !ccCtrl.referred}"></i>
                    </div>
                    <div class="col-sm-6">
                    [properties begin="0" end="4"]
                	<div class="form-group">
                        <label class="control-label" for="[property:name:value:lower]">[property:label:value]</label>
                        <div class="controls">
                            <input type="[property:input:value]" data-ng-model="ccCtrl.[property:name:value:lower]" id="[property:name:value:lower]" class="form-control">
                        </div>
                    </div>
                	[/properties]
                    </div>
                </div>
                [properties begin="5" loops="4"]
                <div class="row">
                	[properties loops="2"]
                	<div class="form-group">
	                    <label class="control-label" for="[property:name:value:lower]">[property:name:value]</label>
	                    <div class="controls">
	                        <input type="text" data-ng-model="ccCtrl.[property:name:value:lower]" id="[property:name:value:lower]" class="form-control">
	                    </div>
	                </div>
                	[/properties]
                </div>
                [/properties]
            </fieldset>
        </form>
    </div>
    <div class="modal-footer">
        <button class="btn btn-success btn-lg" ng-click="ccCtrl.create();ok(create[model:name]Form.$valid)" ng-disabled="create[model:name]Form.$invalid">Save & Close</button>
        <button class="btn btn-warning btn-lg" ng-click="cancel()">Cancel</button>
    </div>

</section>
